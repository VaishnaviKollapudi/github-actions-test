name: Shell Commands
on: [push]
jobs:
  run-shell-command:
      runs-on: windows-latest
      steps:
          - name: echo a string
            run: echo "step-1 hello world"
          - name: multiline script
            run: |
                java -version
                javac -version      